(x) - mariadb 제거
- union 기능
- persistent 기능
- Create Table 기능
(x) - ESM/CJS [ESM(ECMAScript Module:import), CJS(CommonJS:require())]
- where 절에 subquery 기능


- build
    1. npm run build
    2. npm run check
    3. npm run preview
    4. npm run publish
        - 4.1 https://www.npmjs.com/ 사이트 이동 및 Login
        - 4.2 "Access Tokens" 메뉴 선택
        - 4.3 "Generate New Token" 버튼 선택 하여 생성
            (반드시 "Bypass two-factor authentication (2FA) 체크 박스 선택)
        - 4.4 생성된 "npm_..." 키를 "C:\users\사용자ID\.npmrc" 파일에 등록
            "//registry.npmjs.org/:_authToken=npm_..."

        - npm login
        - npm run publish

- git
    git commit -m "select arguments" ./
    git tag -a v1.1.3 -m "select arguments"
    git push origin main
    git push origin v1.1.3
